void compass()
{
  /*
  snittBitFlag ++;
 
      
      snitt [snittBitFlag]; 
    angle = Compass.GetHeadingDegrees();
    angle = angle+ snitt [snittBitFlag] / snittBitFlag;
  
  Serial.println(angle);
  if (angle < 0)
  {
    angle = angle + 255;
  }
  //Serial.println(angle);
if (snittBitFlag ==5)
{
  snittBitFlag =0;
}*/
}
